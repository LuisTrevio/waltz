<div class="hero">
    <div class="hero-text">
        <h1>
            <div class="me">LuisTrevio</div>
            <br>
            Un tipo corriente
        </h1>
        <div class="about-me">
            Hola soy Luis Treviño y me especializo de manera autonoma en el desarrollo de sitios web, de momento estoy estudiando programacion y solo puedo dominar el Front-end, mi meta es ser un desarrollador web Full-Stack.<br>
            <div class="hero-buttons">
                <a href="#" class="button-link-home"><b>Revisar mi CV</b></a>
                <div class="git">
                    <a href="https://github.com/LuisTrevio" class="ghbtn">
                        <div class="svg gh-logo"></div>
                    </a>
                    <a href="#" class="twtbtn">
                        <div class="svg twt-logo"></div>
                    </a>
                    <a href="https://github.com/LuisTrevio" class="twchbtn">
                        <div class="svg twch-logo"></div>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    a {
    text-decoration: none;
    }

    .hero {
        width: 100%;
        height: 100vh;

        max-width: 1130px;

        margin-left: auto;
        margin-right: auto;

        display: flex;

        align-items: center;
        justify-content: left;
    }

    .hero-text {
        display: flex;
        flex-direction: column;
        font-family: 'Quicksand', sans-serif;
        color: var(--white);
        padding: 30px;
    }

    .about-me {
        width: 600px;
       
    }

    h1 {
        font-size: 70px;
        font-family: 'Palanquin Dark', sans-serif;
        color: var(--white);
        line-height: 1.2;
    }

    .me {
        color: var(--nuc);
        display: inline-block;
    }

    
    @media (max-width:780px) {
        .about-me {
            width: 100%;
        }

        h1 {
            font-size: 50px;
        }
    }

    .hero-buttons {
        display: flex;
        flex-direction: row;

        align-items: center;

        gap: 20px;
        margin-top: 20px;
    }

    @media (max-width:359px) {
        .hero-buttons {
            flex-direction: column;
            align-items: flex-start;
        }
    }

    .button-link-home {
        width: fit-content;
        height: auto;

        padding: 12px 16px;

        background: var(--nuc);
        color: var(--white-btn);

        border-radius: 30px;

        font-family: 'Quicksand', sans-serif;

        display: inline-block;
        transition: 0.2s ease-out;
    }

    .button-link-home:hover {
        background: var(--nucl);
    }

    .git {
        width: auto;
        height: 48px;
        border-radius: 30px;

        background: var(--bg-transparente);

        display:flex;
    }

    .twt-logo {
        background-image: url(../img/twitter.svg);
    }
    .gh-logo {
        background-image: url(../img/gh.svg);
    }
    .twch-logo {
        background-image: url(../img/twitch.svg);
    }

    .twtbtn, .ghbtn, .twchbtn {
        width: 48px;
        height: 48px;

        transition: all 0.2s ease-out;
        border-radius: 30px;
    }

    .svg {
        width:48px;
        height: 48px;

        background-repeat: no-repeat;
        background-size: 48% 48%;
        background-position: center;
    }

    .twtbtn:hover {
        background-color:var(--hover);
    }

    .ghbtn:hover {
        background-color:var(--hover);
    }

    .twchbtn:hover {
        background-color:var(--hover);
    }

    @media (prefers-color-scheme: dark) {
        .svg {
            filter: invert(1);
        }
    }

</style>